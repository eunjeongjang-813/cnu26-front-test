# .env.local 파일로 복사 후 사용 (git에 올리지 말 것)
# cp .env.local.example .env.local

# BE 서버 URL (서버 컴포넌트에서만 사용 - 클라이언트에 노출 X)
BACKEND_URL=http://localhost:8080

# 클라이언트에서도 사용하는 환경변수는 NEXT_PUBLIC_ 접두사 필요
# NEXT_PUBLIC_APP_NAME=CNU 쇼핑몰
